<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="User">
        <menu>
            <item android:id="@+id/userP"
                android:title="Profile"
                android:icon="@drawable/person"/>

            <item android:id="@+id/ques"
                android:title="Questions"
                android:icon="@drawable/order"/>

            <item android:id="@+id/newsf"
                android:title="News Feed"
                android:icon="@drawable/ic_notifications"/>

            <item android:id="@+id/revw"
                android:title="Feedbacks"
                android:icon="@drawable/ic_feedback"/>

            <!--<item android:id="@+id/history"
                android:title="History"
                android:icon="@drawable/history"/>-->

        </menu>

    </item>

    <item android:title="App">
        <menu>
            <item android:id="@+id/logoutB"
                android:title="LogOut"
                android:icon="@drawable/ic_logout"/>

            <!--<item android:id="@+id/share"
                android:title="Share"
                android:icon="@drawable/share"/>-->
        </menu>

    </item>

</menu>